<section
  class="standard-form tab scrollable {{tab.cssClass}}"
  data-group="primary"
  data-tab="componentAttributes"
>
  <div class="flexcolumn">
    {{formGroup systemFields.interceptBonus value=system.interceptBonus
    localize=true}} {{formGroup systemFields.interceptCost
    value=system.interceptCost localize=true}} {{formGroup
    systemFields.cameraModifier value=system.interceptCost localize=true}}
    {{formGroup systemFields.refreshCost value=system.interceptCost
    localize=true}} {{formGroup systemFields.hardwareType
    value=system.interceptCost localize=true}}
  </div>
</section>
